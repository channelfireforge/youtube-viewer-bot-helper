// Configuration for the YouTube Viewer Bot
const config = {
    // Replace with your target video URL
    videoUrl: 'https://www.youtube.com/watch?v=dQw4w9WgXcQ',
    // Number of views to simulate
    viewCount: 3,
    // Delay between views in milliseconds (min 10000 to avoid rate limiting)
    delayBetweenViews: 15000
};

module.exports = config;